Few things needing fixing across the app:

- Key Metrics
    - Employer contributions are being counted towards "Your savings rate" on Key Metrics screen
    - Final "remaining for spending" amoung is not aligning between Key Metrics and final Pay Breakdown "All that remains for spending" amount (Yearly view mode)
- Account editor
    - When deleting an account with items associated with it, the app isn't picking up Savings contributions or Loans to be migrated to another account. Should be more dynamic in picking up the associated items with the account, checking all items linked with that "account id"
- Bills view
    - Some rounding issues when setting up a Deduction. Tried adding one for "9.30" but it kept showing as "9.31" once saved. When going to edit screen, it is showing "9.3".
    - In Bill items, amount saved as "9.30" is working fine and shows as "9.30", but when going back to edit it appears as "9.3" (missing final 0 in formatting).
    - Need to limit amount entry decimal spaces to maybe 3, as that's the max that shows in the UI anyway when item is saved.