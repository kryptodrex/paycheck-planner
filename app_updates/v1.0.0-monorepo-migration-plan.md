# v1.0.0 Work Plan

Status keys:
- [ ] Planned
- [-] In Progress
- [x] Done

Release notes discipline:
- [ ] Add a user-facing RELEASE_NOTES.md bullet when each parent item reaches Done.
- [ ] Keep release-note wording focused on outcomes, not implementation details.

---

## 1. Bootstrap Monorepo Workspace (Phase 0)
Parent status: [x] Done

- [x] Add root workspace wiring for apps and packages.
    - Add pnpm workspace config and Turbo pipeline config.
    - Keep existing root scripts available as compatibility aliases.
    - Add workspace-level commands for dev, build, lint, typecheck, test:run, and release-checks.
    - Define top-level pipeline dependencies so package build order is deterministic.
    - Add package filtering commands for app-scoped execution during migration.
- [x] Move the current app into apps/desktop with no user-facing behavior changes.
    - Preserve Electron entry points, Vite config behavior, and existing build outputs.
    - Move renderer, electron process files, build scripts, and static assets in one cut.
    - Keep file path assumptions valid by updating all imports and config include globs.
    - Keep packaged app naming/version behavior unchanged after path migration.
- [x] Introduce shared base TypeScript and lint config package(s) or root-shared configs.
    - Ensure desktop still typechecks and lints after migration.
    - Add a root tsconfig base for strict defaults and per-app overrides.
    - Keep ESLint behavior identical before making any rule changes.
    - Ensure Vitest discovery works after directory relocation.
- [x] Add/update tests where necessary.
    - Verify test discovery still works from workspace root and app scope.
    - Add smoke checks for desktop startup and key app flows after folder migration.

Phase gate checks:
- [x] Running workspace root dev starts desktop successfully.
- [x] Running desktop-scoped dev/build/lint/typecheck/test:run succeeds.
- [x] No release artifact naming/path regressions are introduced.

Done definition:
- [x] apps/desktop runs in dev, builds, and passes lint/typecheck/tests from both app scope and workspace root.

---

## 2. Extract Shared Domain Core (Phase 1)
Parent status: [x] Done

- [x] Create packages/core for pure business and calculation logic.
    - Move portable logic from src/services and src/utils that does not depend on Electron or React runtime APIs.
    - [x] Wave 1a completed: extracted money and currency utilities into packages/core and switched active desktop imports.
    - [x] Wave 1b completed: extracted frequency and payPeriod utilities into packages/core and switched desktop imports to core subpaths.
    - [x] Wave 1c completed: extracted taxLines and historyEngine utilities into packages/core and switched desktop imports to core subpaths.
    - Start with money/frequency/tax/history/reallocation utilities as first extraction wave.
    - Keep package structure grouped by domain modules, not by legacy folder names.
    - Move shared data/constants used by calculations into core-owned modules.
- [x] Create a stable public API surface for core.
    - Add barrel exports and explicit package exports to avoid deep-import coupling.
    - [x] Define one entrypoint for runtime exports and one for type-only exports if needed.
    - Mark internal-only modules as non-exported and enforce via lint rules.
- [x] Establish hard boundary rules.
    - Prevent core from importing Electron, window globals, and UI modules.
    - [x] Add static checks to prevent React/Electron/browser-only imports in core.
    - [x] Add package dependency direction rules to avoid reverse coupling.
- [x] Port and expand tests for moved logic.
    - Preserve existing coverage and add regression tests for key financial calculations.
    - [x] Core parity suites now exist for money, currency, frequency, payPeriod, taxLines, and historyEngine.
    - [x] Add golden tests for known calculation scenarios to catch cross-app drift.
    - [x] Add round-trip tests for currency/frequency/display-mode conversion paths.
- [x] Add/update tests where necessary.

Sequencing notes:
- [x] Move code first, then update imports, then run tests, then remove old copies.
- [x] Avoid mixed refactor plus behavior changes in the same PR.
- [x] Merge in small vertical slices to reduce migration risk.

Phase gate checks:
- [x] desktop compiles using core package imports for extracted modules.
- [x] calculation parity tests pass between pre-extraction and post-extraction outputs.

Done definition:
- [x] Desktop app consumes core package imports for extracted logic, with no behavior regressions in existing financial calculations.

---

## 3. Introduce Platform and Storage Adapters (Phase 2)
Parent status: [x] Done

- [x] Create packages/storage with interface-first contracts.
    - [x] Define plan persistence and secure key storage interfaces.
    - [x] Define repository-style interfaces for load/save/encryption key lifecycle.
    - [x] Define normalized error types for file-missing, permission, and decryption failures.
    - [x] Add adapter contract tests shared by all storage implementations.
- [x] Create packages/platform-electron for Electron-specific adapters.
    - Move file dialog/file I/O and keychain integrations behind adapter implementations.
    - [x] Initial keychain adapter implemented and wired in desktop through `KeychainService`.
    - [x] Initial file dialog/file I/O adapter implemented and wired in desktop through `FileStorageService`.
    - [x] Move electron API wrapper logic into adapter modules with typed contracts.
    - Keep renderer unaware of ipc channel names and process internals.
- [x] Refactor desktop service wiring to depend on interfaces.
    - Use dependency injection/composition at app boundary.
    - Centralize adapter assembly in one composition root for easier testing.
    - Keep current context API surface stable while internals are swapped.
- [x] Add boundary-safe error handling and fallback behavior.
    - Preserve current file relink and save/load safety behavior.
    - Keep moved-file relink flow behavior unchanged for existing users.
    - Preserve user-safe error messaging for keychain and encryption failures.
- [x] Add/update tests where necessary.
    - Add adapter and contract tests (mocking Electron APIs).
    - Add integration tests for file open/save/relink and key retrieval paths.

Phase gate checks:
- [x] no core imports from electron/preload/renderer-specific modules.
- [x] desktop persistence and keychain flows pass existing and new tests.

Done definition:
- [x] Core package is platform-agnostic, while desktop-specific capabilities are isolated in platform-electron and satisfy storage contracts.

---

## 4. Desktop App Stabilization on New Boundaries (Phase 2.5)
Parent status: [x] Done

- [x] Update desktop composition root to wire core + storage + platform adapters.
    - Keep current UX and keyboard reliability model intact.
    - Preserve global shortcut two-tier behavior and menu-event fallbacks.
- [x] Validate moved-file relink, encryption setup, and save/open flows end-to-end.
    - [x] Validate recoverability from stale paths, missing files, and wrong passwords.
- [x] Remove obsolete legacy imports and dead code paths.
    - Remove temporary compatibility shims after parity is proven.
    - Ensure no duplicated business logic remains in app layer.
- [x] Add/update tests where necessary.
    - [x] Add focused integration tests around BudgetContext wiring and persistence paths.
    - [x] Add regression tests around history/undo and transient status messaging.

Stabilization checkpoints:
- [x] baseline performance and startup times are within acceptable variance.
- [x] critical user journeys complete with no UX regressions.
- [x] release packaging still works for all desktop targets currently supported.

Done definition:
- [x] Desktop feature parity is maintained after boundary refactor, including encryption, file relink handling, and history behavior.

---

## 5. Stand Up API App (Phase 3)
Parent status: [-] In Progress

- [x] Create apps/api with minimal production-ready skeleton.
    - Health endpoint, version endpoint, validation endpoint, and currency proxy route.
    - Canonical health/version routes, shared validation, and a currency proxy are live.
    - [x] Add request schema validation and consistent error response envelope.
    - Add structured logging, request correlation id, and basic security middleware.
- [x] Reuse core validation/transformation logic in API routes.
    - Ensure identical business rules across desktop and API.
    - Reuse shared domain types for request and response contracts.
    - [x] Add parity tests proving API calculation outputs match core direct invocation.
- [ ] Add persistence and auth strategy placeholders.
    - Define environment contracts and secret management approach.
    - Add auth abstraction that supports initial shared-secret mode and future user auth.
    - Define storage boundary for future sync/versioning workflows.
- [ ] Add/update tests where necessary.
    - [x] Add route tests for health, version, versioned routes, and validation parity.
    - Add core parity tests for shared rule paths.
    - Add failure-path tests for invalid payloads, unauthorized requests, and timeouts.

Phase gate checks:
- [x] api can run independently in dev and in production build mode.
- [x] endpoint contracts are documented and validated by tests.

Done definition:
- [ ] apps/api builds and runs independently, and shared logic parity is verified via tests.

---

## 6. Stand Up Mobile App Foundation (Phase 4)
Parent status: [ ] Planned

- [ ] Create apps/mobile scaffold with chosen stack.
    - Implement initial app shell and navigation.
    - Establish shared design token strategy for cross-platform consistency.
    - Add strict typing and lint/test config from workspace standards.
- [ ] Integrate core package for read-only plan views first.
    - Prioritize correctness of rendered calculations over full edit flows.
    - Implement initial read-only screens for summary, paycheck breakdown, and accounts.
    - Validate display-mode and currency formatting parity with desktop output.
- [ ] Add mobile storage adapter implementation.
    - Support offline-first local persistence for initial milestone.
    - Define migration/versioning strategy for local plan schema updates.
    - Add corruption handling and safe-recovery behavior for invalid local data.
- [ ] Add/update tests where necessary.
    - Add smoke tests and core-consumption tests for critical screens.
    - Add snapshot or contract tests for serialized local plan payload shape.

Phase gate checks:
- [ ] mobile app runs independently and can load sample plans using core.
- [ ] offline persistence survives app restart and schema migration.

Done definition:
- [ ] apps/mobile runs independently with read-only budget views powered by shared core logic.

---

## 7. CI/CD and Independent Deployability (Phase 5)
Parent status: [ ] Planned

- [ ] Implement affected-scope task execution in CI.
    - Run lint/typecheck/tests/build only for changed apps/packages plus dependencies.
    - Add branch protection checks for critical tasks per affected project.
    - Add fallback full-repo validation workflow for release branches.
- [ ] Split release pipelines by deploy target.
    - Desktop installer pipeline, API deploy pipeline, mobile build pipeline.
    - Define trigger rules so unrelated app changes do not block each release path.
    - Add environment-specific secrets and signing credentials isolation.
- [ ] Add cache strategy for monorepo tasks.
    - Enable local/remote cache where appropriate.
    - Track cache hit ratio and CI duration metrics before/after enablement.
- [ ] Add/update tests where necessary.
    - Add CI checks that fail on cross-package boundary violations.
    - Add workflow tests for expected artifacts and naming conventions.

Phase gate checks:
- [ ] each app/package can be built, tested, and released without invoking unrelated deploy steps.
- [ ] release artifacts are reproducible and traceable to commit/version metadata.

Done definition:
- [ ] Desktop, API, and mobile can be built and released independently without blocking unrelated changes.

---

## 8. Architecture Guardrails and Developer Experience (Phase 6)
Parent status: [ ] Planned

- [ ] Document allowed dependency directions.
    - Example: apps -> packages/core; packages/core must not depend on apps/platform code.
    - Publish architecture diagrams and ownership boundaries for each package.
    - Define codeowner responsibilities for shared packages.
- [ ] Add import boundary lint rules and workspace scripts.
    - Prevent accidental deep imports and cross-layer leaks.
    - Add deny-lists for forbidden imports by package type.
    - Add script to detect and fail on circular workspace dependencies.
- [ ] Add scaffolding guidance for future apps/packages.
    - Ensure new modules follow naming, config, and testing conventions.
    - Add new-package template with default tsconfig, lint, vitest, and package exports.
    - Add contributor quickstart for common workspace commands.
- [ ] Add/update tests where necessary.
    - Add policy tests/automation that validate boundary rules continuously.

Long-term maintenance checks:
- [ ] quarterly dependency and workspace config audit is documented.
- [ ] release checklist includes monorepo boundary and parity verification.

Done definition:
- [ ] Repository enforces architecture boundaries automatically and includes clear contributor workflow docs for monorepo development.

---

## Suggested PR Breakdown (Execution Order)
Parent status: [ ] Planned

- [ ] PR 1: Workspace scaffold and desktop move with zero behavior changes.
- [ ] PR 2: Core package extraction wave 1 (pure utilities + tests).
- [ ] PR 3: Core package extraction wave 2 (calculation services + tests).
- [ ] PR 4: Storage contracts and electron adapter introduction.
- [ ] PR 5: Desktop composition refactor and stabilization.
- [ ] PR 6: API skeleton and shared contract parity tests.
- [ ] PR 7: Mobile scaffold and read-only core-backed views.
- [ ] PR 8: CI/CD split pipelines, caching, and guardrail automation.

Done definition:
- [ ] Each PR remains reviewable, shippable, and independently reversible.

---

## Final v1.0.0 Exit Checklist
- [ ] All parent items above are marked [x] Done or explicitly deferred.
- [ ] RELEASE_NOTES.md updated with completed v1.0.0 user-facing items.
- [ ] Lint, typecheck, tests, and build pass for merged changes.
