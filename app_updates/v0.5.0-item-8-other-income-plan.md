# v0.5.0 Item 8 Plan: Other Income Sources

Status keys:
- `[ ]` Planned
- `[-]` In Progress
- `[x]` Done

Parent item: Item 8 in v0.5.0-list.md
Parent status: `[-] In Progress`

---

## Goal
Add support for income sources beyond the base salary/hourly paycheck while keeping pay calculations, taxes, summaries, exports, search, and audit history understandable and deterministic.

## Scope Decision for v0.5.0
- In scope: Fixed-amount other income entries with frequency, income type, notes, enable/disable, and taxability controls.
- In scope: Percentage-of-gross income rules for bonus-style income.
- In scope: Automated withholding presets by income type with manual override preserved.
- In scope: Clear income-impact treatment so entries can affect gross pay, taxable income only, or net pay only depending on the income source.
- In scope: Inclusion of other income impacts in annual, monthly, and per-paycheck summary views.
- In scope: History/audit coverage, search integration, save/load/migration support, and demo-data/new-plan compatibility.
- Out of scope for v0.5.0 unless explicitly approved later: Country-specific tax engines for all jurisdictions tied to other-income type.
- Out of scope for v0.5.0 unless explicitly approved later: Fully date-effective payout schedules beyond simple frequency/month-window controls.

---

## Product Model

### Supported Income Types
- [x] Add built-in income types for Bonus, Commission, Rental Income, Retirement Withdrawal, Disability, Reimbursement/Allowance, Investment Income, and Other.
- [-] Allow a custom label when `incomeType = 'other'` or when users want a payroll-source-specific name. _(The `name` field serves this implicitly; no dedicated UX hint when type is 'other'.)_

### Income Impact Modes
- [x] Add a required income-impact field, such as `payTreatment: 'gross' | 'taxable' | 'net'`.
- [x] Define `gross` treatment for payroll-like income that should increase gross pay first, then flow through taxable income and taxes.
- [x] Define `taxable` treatment for supplemental income that should affect withholding/taxable wages but remain visually separate from base paycheck gross.
- [x] Define `net` treatment for after-tax income that should increase take-home totals without affecting taxable income or withholding.
- [x] Document which built-in income types default to each treatment and allow manual override when needed.

### Withholding Behavior
- [x] Add per-entry withholding mode, such as `manual | auto | none`, with sane defaults by income type and treatment. _(Field and UI exist; `auto` is saved but not yet computed — see Workstream C.)_
- [ ] Ensure `auto` mode can propose withholding deltas without overwriting the user's existing manual tax lines. _(Not yet implemented — Workstream C.)_
- [x] Ensure `none` mode is available for net-only and user-managed tax scenarios.

### Calculation Principles
- [x] Keep calculations deterministic and explainable across mixed base pay + other income scenarios.
- [x] Preserve the current bias toward conservative/over-estimated withholding instead of under-estimated take-home.
- [x] Keep manual user edits authoritative whenever automated logic would otherwise conflict.

---

## Workstream A: Core Other-Income Model and Data Layer

### Data Model
- [x] Add a new `OtherIncome` type with fields for `id`, `name`, `incomeType`, `amountMode`, `amount`, `percentOfGross`, `frequency`, `enabled`, `notes`, `isTaxable`, `payTreatment`, `withholdingMode`, and optional `withholdingProfileId`.
- [x] Add optional scheduling controls for expanded scope, such as `activeMonths?: number[]`. _(Field exists in type and normalized in fileStorage; no form UI yet — see Defaulting/Validation.)_
- [ ] Add optional guardrails for percent-based entries, such as cap/floor fields if needed.
- [x] Add `otherIncome?: OtherIncome[]` to `BudgetData`.
- [x] Add migration defaults so existing plans load with `otherIncome: []`.
- [x] Update runtime validation/type guards to safely load and validate the new structure. _(Type guards and normalizers for all 6 enum fields in fileStorage.ts.)_
- [x] Add migration and backwards-compatibility tests.

### Tab view
- [x] Add a new tab labeled as "Other Income" with structure similar to other tabs
    - Use the 'BanknoteArrowUp' icon
- [x] This tab should be structured appropriately for these new income sources, using the similar SectionCardItem components from other views
- [x] The tab by default should be hidden as it won't be the most common to use, and user can choose to 'show' it from the Manage Tabs modal.

### Defaulting and Validation Rules
- [x] Require non-negative values for fixed amounts, percent rules, and optional caps/floors.
- [ ] Restrict percent-of-gross entries to sensible ranges and document the final guardrails. _(No cap/floor validation yet.)_
- [x] Require explicit frequency for every other-income entry.
- [x] Require explicit `payTreatment` so each entry's effect on calculations is unambiguous.
- [ ] Validate that active-month schedules are unique, in-range, and compatible with the selected frequency. _(No activeMonths UI or schedule validation yet.)_

---

## Workstream B: Calculation Engine and Income Placement Rules

### Frequency and Annualization
- [x] Add shared utilities to convert other-income entries into annual, monthly, and per-paycheck equivalents. _(`src/utils/otherIncome.ts`)_
- [x] Support deterministic handling for annual, quarterly, monthly, and per-paycheck income frequencies.
- [x] Document rounding strategy for line-level and aggregate calculations. _(Policy settled: intermediate per-paycheck amounts are not rounded; rounding only at final display/annual presentation boundaries.)_

### Fixed-Amount Entries
- [x] Integrate fixed-amount other income into the pay calculation pipeline.
- [x] Ensure `gross` entries increase gross pay before pre-tax deductions and taxes.
- [x] Ensure `taxable` entries increase taxable income and withholding calculations while remaining distinct from base paycheck gross in presentation.
- [x] Ensure `net` entries increase take-home amounts only and do not affect taxable income or tax calculations.

### Percentage-of-Gross Rules
- [x] Add `amountMode: 'fixed' | 'percent-of-gross'`.
- [x] Resolve the percent base from the base paycheck gross before other-income additions unless a future basis override is explicitly approved.
- [x] Compute percent-based entries in a deterministic order that prevents circular references.
- [x] Add preview helpers so the UI can show computed per-paycheck/month/year amounts before save.
- [x] Add focused tests for mixed fixed + percent income stacks.

### Tax and Withholding Interaction
- [x] Define how taxable other income contributes to the existing tax-estimation and tax-line model.
- [ ] For `gross` and `taxable` entries, compute withholding deltas separately from base paycheck taxes so the source of withholding remains explainable. _(Blocked on Workstream C auto-withholding implementation.)_
- [x] Keep current manual tax lines authoritative unless a dedicated auto-withholding path is enabled.
- [ ] Make sure auto-withholding suggestions add deltas predictably rather than silently mutating unrelated tax lines. _(Not yet implemented — Workstream C.)_
- [x] Ensure `net` entries never inflate gross/taxable displays or withholding totals.

---

## Workstream C: Automated Withholding Presets

### Preset Model
- [ ] Add default withholding profiles by income type, such as supplemental bonus behavior, retirement-withdrawal withholding, or no-withholding defaults for non-payroll income.
- [x] Add optional `withholdingProfileId` to other-income entries. _(Field exists on type; always `undefined` in practice until profiles are defined.)_
- [ ] Keep profile mapping internal/testable so the UI only consumes structured profile metadata.

### Calculation Behavior
- [x] Add `withholdingMode: 'manual' | 'auto' | 'none'` per entry or equivalent plan-safe design. _(Field, type guard, normalizer, and UI mode selector all present. `auto` behavior is saved but not yet computed.)_
- [ ] In `auto` mode, generate withholding suggestions from the selected/default profile.
- [x] In `manual` mode, let the user enter or leave withholding impact fully under their control.
- [x] In `none` mode, record the income without any automatic withholding impact.
- [ ] Ensure auto-generated withholding is visible as a distinct source in breakdowns and explain views.

### Explainability and Safety
- [ ] Add a trace/explain structure: income source, treatment mode, withholding source, taxable base, applied rate, and estimated withheld amount.
- [x] Preserve a one-click fallback to manual control if the user dislikes or distrusts auto behavior. _(Withholding mode selector allows switching to manual at any time.)_
- [ ] Add regression tests ensuring manual tax lines are not overwritten unexpectedly. _(Blocked on Workstream C implementation.)_

---

## Workstream D: UI and UX

### Other Income Management UI
- [x] Add an `OtherIncomeManager` tab view using existing manager patterns.
- [x] Add create/edit/delete/enable-disable flows for other-income entries.
- [-] Add validation UI for required fields, non-negative values, and schedule conflicts. _(Name and amount/percent validation done; no schedule-conflict validation since activeMonths UI is not yet built.)_
- [x] Show treatment clearly in the form so users understand whether an income source affects gross, taxable income, or net pay.

### Entry Form Behavior
- [x] Add toggle between fixed amount and % of gross for eligible entries.
- [-] Add frequency selector and optional active-month selector for irregular income. _(Frequency selector done; active-month selector not yet implemented.)_
- [x] Add treatment selector with helper text for `Gross`, `Taxable Only`, and `Net Only`.
- [-] Add withholding mode selector and profile selector where relevant. _(Withholding mode selector done; profile selector not implemented since profiles are not yet defined.)_
- [x] Show real-time previews for per-paycheck, monthly, and annual impact.
- [ ] Warn when the chosen frequency/month-window combination creates sparse or irregular payouts.

### Summary and Breakdown Presentation
- [x] Show other-income contributions in key summary views without obscuring base pay.
- [x] Make sure `taxable-only` income is displayed distinctly enough that gross-pay reconciliation still makes sense. _(Separate 'Taxable additions' section in Pay Breakdown.)_
- [x] Add breakdown rows or grouped sections so users can understand how each income source affects totals. _(Pay Breakdown shows per-entry line items in gross/taxable/net sections.)_
- [-] Ensure tax views can show which tax impacts come from other income vs base paycheck where practical. _(Sections are visually separated in Pay Breakdown, but per-tax-line attribution is not yet implemented.)_

---

## Workstream E: Integration, Search, Export, and History

### Integration
- [x] Wire tab visibility defaults and tab management support.
- [ ] Ensure exports and PDFs include other-income impacts where relevant.
- [x] Ensure demo/new plan initialization includes a realistic empty/default `otherIncome` field.
- [x] Ensure encryption/save/load paths include the new field.

### Search and Navigation
- [x] Add search integration entries and deep-link actions for Other Income. _(`src/utils/searchModules/otherIncomeSearchModule.ts` — per-entry results with toggle/edit/delete/add actions.)_
- [x] Support search terms for income type, treatment, withholding mode, and manager actions where appropriate.

### Audit and History
- [x] Add history/audit integration for create/update/delete/enable-disable actions.
- [x] Ensure audit entries summarize whether a change affected gross, taxable-only, or net-only income behavior.

---

## Testing Plan

### Unit Tests
- [x] Unit tests for frequency conversion and annualization logic.
- [x] Unit tests for fixed vs percent-of-gross amount calculation.
- [ ] Unit tests for month-window behavior. _(activeMonths UI and validation not yet built.)_
- [x] Unit tests for gross vs taxable-only vs net-only treatment.
- [ ] Unit tests for withholding profile behavior and mode selection. _(Blocked on Workstream C.)_

### Integration and Regression Tests
- [x] Regression tests for mixed base pay + other income + tax scenarios.
- [x] Regression tests ensuring `taxable-only` entries affect taxes without corrupting base gross displays.
- [x] Regression tests ensuring `net-only` entries bypass tax logic entirely.
- [x] Regression tests verifying existing plans still load without data loss.
- [ ] Regression tests ensuring manual tax lines are not overwritten unexpectedly. _(Blocked on Workstream C.)_

### UI and Workflow Tests
- [x] Component tests for add/edit/delete/toggle flows. _(Minimal — add entry and render entry covered.)_
- [-] Component tests for amount-mode toggles, previews, and treatment selection. _(Amount-mode and treatment present in mock data assertions; monthly/annual preview and activeMonths not tested.)_
- [x] Search/action tests for opening and focusing the correct UI targets.
- [ ] Explainability tests for withholding preview/output if an explain panel is added. _(Blocked on Workstream C.)_

---

## Risks
- Risk: Adding `taxable-only` income can create confusing gross vs taxable reconciliations.
- Mitigation: Present base gross and supplemental taxable income separately in summaries and breakdowns.
- Risk: Auto-withholding may reduce user trust if it silently changes take-home pay.
- Mitigation: Keep manual override explicit, show previews, and expose the withholding source/rate/base.
- Risk: Percent-of-gross rules can introduce circular or order-dependent math.
- Mitigation: Lock the percent base to base paycheck gross and test deterministic ordering.
- Risk: Scope expansion may create regressions in core paycheck calculations.
- Mitigation: Add focused regression coverage for mixed income/tax scenarios before release cut.

## Dependencies
- Depends on stable budget migration and validation paths in file storage.
- Depends on calculation services that can support additional income inputs without destabilizing existing paycheck flows.
- Depends on tab/search registration updates for discoverability.
- Depends on export and audit layers being able to surface the new income impacts clearly.

---

## Acceptance Criteria
- [x] Users can add, edit, pause, and delete other income entries and view their history.
- [x] Users can choose whether an entry affects gross pay, taxable income only, or net pay only.
- [x] Users can configure fixed-amount and % of gross income rules with clear previews.
- [ ] Automated withholding presets are available where relevant, transparent, and manually overridable. _(Workstream C not yet implemented.)_
- [x] Other income appears in relevant annual, monthly, and per-paycheck totals without obscuring base-pay behavior.
- [x] Other income entries are tracked in the audit log.
- [x] Existing plans open normally with no migration issues.
- [-] Tests cover core math, UI interactions, tax interaction behavior, and migration paths. _(Core math, basic UI, and migration covered; auto-withholding tests and activeMonths tests remain.)_

## Done Definition
- [-] Other-income management is available and stable in v0.5.0. _(Core feature is live; pending: activeMonths UI, Workstream C, PDF export.)_
- [-] Fixed-amount, % of gross, and withholding-preset workflows are complete or explicitly deferred before release cut. _(Fixed-amount and % of gross done. Withholding presets not yet implemented — requires explicit defer decision before release cut.)_
- [x] Gross/taxable-only/net-only treatment behavior is documented, implemented, and covered by tests.
- [-] All planned tests for this item pass. _(Existing tests pass; Workstream C and activeMonths tests not yet written.)_
- [ ] Release notes include a user-facing outcome summary for Item 8.
